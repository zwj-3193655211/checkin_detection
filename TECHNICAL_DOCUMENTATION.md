# 晨读晨练签到打卡检测系统 — 详细技术文档

> 本文档详细记录了本项目从初始方案探索到最终方案确定的完整实验历程，包括每个阶段的方案选择、实验结果、失败原因分析以及最终优化路径。整个开发过程历经数月，手工标注超过2000张图片，经历了多次方案推翻与重构，最终找到了一条可靠的技术路线。

---

## 目录

1. [项目概述](#1-项目概述)
2. [实验历程：从失败到成功](#2-实验历程从失败到成功)
   - 2.1 [阶段一：ResNet18 端到端分类](#21-阶段一resnet18-端到端分类)
   - 2.2 [阶段二：ResNet50 端到端分类](#22-阶段二resnet50-端到端分类)
   - 2.3 [阶段三：CLIP 零样本分类](#23-阶段三clip-零样本分类)
   - 2.4 [阶段四：CLIP + 单 MLP](#24-阶段四clip--单-mlp)
   - 2.5 [阶段五：CLIP + 双 MLP + 三支决策（最终方案）](#25-阶段五clip--双-mlp--三支决策最终方案)
3. [数据集与标注工作](#3-数据集与标注工作)
4. [技术架构](#4-技术架构)
5. [模型设计详解](#5-模型设计详解)
6. [温度缩放（Temperature Scaling）](#6-温度缩放temperature-scaling)
7. [四规则三支决策系统](#7-四规则三支决策系统)
8. [训练流程](#8-训练流程)
9. [实验结果](#9-实验结果)
10. [系统实现](#10-系统实现)
11. [部署说明](#11-部署说明)
12. [总结与反思](#12-总结与反思)

---

## 1. 项目概述

### 1.1 项目背景

高校晨读晨练签到管理是一项日常但重要的工作。学生每天需上传打卡照片（晨读照片或晨跑照片），管理员需要逐一审核这些照片是否合规。随着参与人数增加，人工审核面临严峻挑战：

- **效率低下**：每日需审核数百至上千张打卡照片，耗费大量人力
- **标准不一**：不同审核人员对"异常"的判断标准存在主观差异
- **成本高昂**：需要安排专人负责审核工作，挤占了教学管理资源

因此，亟需一种自动化检测方案，能够自动筛选出异常照片，只将少量不确定的样本交给人工复审。

### 1.2 项目目标

| 目标 | 指标 | 核心含义 |
|------|------|---------|
| **零漏检** | 漏检率 = 0% | 所有异常图片必须被拦截，一个都不能放过 |
| **低审核率** | 人工审核率 < 30% | 尽量减少需要人工确认的图片数量 |
| **高准确率** | 分类准确率 > 95% | 自动通过的图片分类基本正确 |
| **可解释性** | 支持决策依据 | 不能是"黑箱"，每张图的判断必须能给出理由 |

其中，**零漏检**是最核心也最严格的要求——在签到场景下，任何一张异常照片的漏放都意味着有人可能作弊成功，这是不可接受的。

---

## 2. 实验历程：从失败到成功

> 这一部分完整记录了我在项目开发过程中的技术选型与迭代过程。每一个"失败"的方案都让我对问题有了更深的理解，最终指引我找到了正确的道路。

### 2.1 阶段一：ResNet18 端到端分类

**时间**：项目初期

**方案描述**：

最初，我采用了最直觉的方法——用经典的 ResNet18 卷积神经网络，直接对图片进行三分类（晨读 / 晨跑 / 异常）。这是深度学习图像分类的标准范式：数据增强 → CNN特征提取 → 全连接分类头 → softmax → 交叉熵损失。

**实验配置**：
- 模型：ResNet18（预训练 ImageNet，微调最后两层）
- 输入：224×224 RGB 图片
- 分类：三分类（晨读=0, 晨跑=1, 异常=2）
- 优化器：Adam, lr=3e-4
- 训练轮数：50 epochs
- 数据增强：随机水平翻转、颜色抖动

**实验结果**：

| 指标 | 数值 |
|------|------|
| 整体测试准确率 | 94.12% |
| 晨读类准确率 | ~96% |
| 晨跑类准确率 | ~92% |
| 异常类准确率 | **33.33%** |
| 漏检率 | **66.7%** |

**结果分析——为何失败**：

表面上看 94.12% 的准确率还不错，但这个数字极具欺骗性。由于数据集极度不平衡（晨读1491张、晨跑525张、异常仅24张），模型学会了"偷懒"——它几乎把所有样本都预测为晨读或晨跑，因为这样就能轻松获得高准确率。

异常类准确率仅 33.33%，意味着 24 张异常图片中有 16 张被漏放。漏检率高达 66.7%，这在签到检测场景下是完全不可接受的。

**根本原因分析**：

1. **极端的类别不平衡**：异常样本仅占 1.2%，模型在训练中几乎"看不到"异常类，导致决策边界对异常样本不敏感
2. **异常类的内部分异性**：异常包含"太暗""没有人""场景不对""背景模糊"等多种截然不同的情况，它们在特征空间中并不聚拢，而是分散在各个位置，很难被一个统一的"异常"类所覆盖
3. **交叉熵损失的固有缺陷**：标准交叉熵损失在类别不平衡时会被大类主导，少数类的梯度信号被淹没

**尝试的补救措施（均无效）**：
- 加权交叉熵（给异常类更大权重）：模型开始在正常样本上产生大量误判
- 过采样异常类：24 张图片翻倍后仍然太少，且容易过拟合
- Focal Loss：略有改善，但异常类准确率仍在 40% 以下

**结论**：直接端到端三分类走不通，必须换思路。

---

### 2.2 阶段二：ResNet50 端到端分类

**方案描述**：

ResNet18 失败后，我怀疑是模型容量不够，于是尝试了更深的 ResNet50，并额外尝试了二分类变体（只区分晨读/晨跑，不直接识别异常）。

**实验配置**：
- 模型：ResNet50（预训练 ImageNet，微调）
- 三分类版本和二分类版本均训练
- 优化器：Adam, lr=2e-4
- 训练轮数：80 epochs
- 数据增强：随机水平翻转、颜色抖动、随机裁剪

**实验结果**：

| 方案 | 整体准确率 | 异常类准确率 | 漏检率 |
|------|-----------|-------------|--------|
| ResNet50 三分类 | 95.3% | 37.5% | 62.5% |
| ResNet50 二分类 | 96.8% | — | — |

**结果分析——为何仍失败**：

更大的模型（ResNet50）并没有解决核心问题——异常样本太少了。三分类的异常类准确率从 33.33% 提升到了 37.5%，仍然是杯水车薪。

二分类版本虽然正常样本的准确率很高，但它根本无法识别异常——因为训练时没见过异常，推理时异常样本会被强制分到晨读或晨跑中某一类，无法被拦截。

**根本问题**：无论 ResNet18 还是 ResNet50，从头在小数据集上训练 CNN 都面临同样的困境——数据太少、类别太不平衡，模型无法学到异常的泛化特征。

**结论**：需要预训练特征提取能力更强的模型，而不是更大的模型。

---

### 2.3 阶段三：CLIP 零样本分类

**方案描述**：

在 ResNet 方案失败后，我将目光转向了 CLIP（Contrastive Language-Image Pre-training）模型。CLIP 是 OpenAI 在 4 亿图文对上训练的视觉-语言模型，具有强大的零样本分类能力——不需要任何训练数据，只需构造文本提示（prompt），就能让模型判断图片与哪个文本描述最匹配。

我构造了以下文本提示：

```python
# 晨读相关提示
prompts_read = [
    "a photo of students reading in a classroom",
    "晨读场景，学生在教室里读书",
    "教室内的晨读照片",
]

# 晨跑相关提示
prompts_run = [
    "a photo of students running on a track",
    "晨跑场景，学生在操场上跑步",
    "操场上的晨跑照片",
]

# 异常相关提示
prompts_abnormal = [
    "a dark or blurry photo",
    "一张过暗或模糊的照片",
    "不在教室或操场的异常照片",
]
```

**实验配置**：
- 模型：CLIP ViT-B/32
- 方法：计算图片特征与各类文本提示特征的余弦相似度，取最高分类别
- 无需训练，零样本推理

**实验结果**：

| 指标 | 数值 |
|------|------|
| 整体准确率 | 76.35% |
| 晨读类准确率 | ~82% |
| 晨跑类准确率 | ~70% |
| 异常类准确率 | **41.67%** |
| 漏检率 | **58.3%** |

**结果分析——为何失败**：

CLIP 零样本分类的整体准确率只有 76.35%，比 ResNet 还差。原因有几个：

1. **语义鸿沟**：我构造的文本提示难以精确描述"异常"这一概念。"异常"不是一个语义类别，而是一个"非正常"的否定性描述。CLIP 的对比学习范式天然擅长匹配肯定性语义（"这是猫"），而不擅长处理否定性语义（"这不是正常的签到照"）

2. **提示工程的局限性**：我尝试了大量不同的提示模板（中英文混合、不同描述风格），但 CLIP 的图文匹配分数对"异常"的区分度始终很低。异常图片（如过暗的照片）可能仍然与"教室"有一定的语义关联

3. **缺乏领域适应**：CLIP 是在通用图文对上训练的，对"晨读签到""号码布"等校园场景概念的理解有限。直接零样本使用，没有利用到任何领域数据

**结论**：CLIP 的特征提取能力很强，但零样本分类不适合这个任务。需要利用 CLIP 的特征提取能力，但在其之上构建适合本任务的分类器。

---

### 2.4 阶段四：CLIP + 单 MLP

**方案描述**：

这是关键的转折点。我意识到应该将 CLIP 作为**特征提取器**而非分类器——先冻结 CLIP 的权重，提取图片的 512 维特征向量，然后训练一个轻量级的 MLP 分类器。

具体方案：
1. 用 CLIP ViT-B/32 提取每张图片的 512 维特征向量（固定不变，只提取一次）
2. 训练一个三分类 MLP：`512 → 256 → 128 → 3`（晨读/晨跑/异常）
3. 使用交叉熵损失 + 加权处理类别不平衡

**实验配置**：
- 特征提取器：CLIP ViT-B/32（冻结，不训练）
- 分类器：3 层 MLP，512→256→128→3
- 优化器：Adam, lr=0.001
- 训练轮数：100 epochs
- 数据集：完整训练集（含异常样本）

**实验结果**：

整体准确率有了显著提升（~97%），但异常类仍然是瓶颈。由于异常样本只有 24 张，即使有 CLIP 特征加持，MLP 仍然难以在特征空间中为异常类划出可靠的决策边界。

**关键问题——可解释性差**：

这个阶段暴露了一个更深层的问题：**单 MLP 只输出一个类别标签，无法解释为什么做出这个判断**。在实际部署中，管理员需要知道"为什么这张图被判为异常"或"为什么这张图可以自动通过"。一个只能输出"晨读"或"晨跑"的黑箱模型，即使准确率再高，也无法满足实际业务的可解释性需求。

**老师的建议——拆分预测任务**：

正是在这个阶段，指导老师给了我一个关键的建议：

> "与其让模型直接判断'这张图是否正常'这种大而笼统的任务，不如让模型分别判断图中的各个要素是否存在——比如有没有人脸、有没有跑道、有没有教室。这样就把一个复杂的大预测任务，拆分成了多个简单的子任务。"

这个建议启发了我：

- 如果模型能识别出"图中没有人脸"，那这很可能是"没有人"的异常
- 如果模型预测为"晨跑"但"图中没有跑道也没有号码布"，那预测值得怀疑
- 每个要素的预测都是独立可解释的，组合起来就能形成决策依据

**结论**：单 MLP 不可行，需要引入第二个 MLP 来预测 11 维特征，实现可解释性。

---

### 2.5 阶段五：CLIP + 双 MLP + 三支决策（最终方案）

**方案描述**：

在老师的建议启发下，我设计了最终的双 MLP 架构：

1. **MLP 主分类器**：二分类（晨读 / 晨跑），**训练时不包含异常样本**
2. **MLP 特征预测器**：11 维多标签预测，预测图中各要素是否存在
3. **四规则三支决策**：组合两个 MLP 的输出，通过规则引擎决定"自动通过"或"待审核"

**核心设计理念**：

- **二分类而非三分类**：训练集只包含正常样本（晨读+晨跑），模型只需要学会区分两种正常场景，不需要学习"异常"这个模糊的概念
- **异常由规则识别而非模型识别**：异常图片进入 MLP 后，要么被分到某个类但置信度很低（因为训练时没见过异常），要么匹配到的特征很少（因为异常图片缺少正常要素），从而被规则引擎拦截
- **可解释的决策**：每张图的判定都有 11 维特征概率作为依据，管理员可以清楚看到"这张图被拦截是因为置信度只有 0.72"或"匹配特征只有 2 个"

**实验结果**：

| 指标 | 数值 | 目标 | 状态 |
|------|------|------|------|
| 漏检率 | 0.00% | 0% | ✅ |
| 人工审核率 | 22.83% | <30% | ✅ |
| 正常通过率 | 77.17% | >70% | ✅ |
| 分类准确率 | ~99% | >95% | ✅ |

**终于成功！** 所有异常样本均被拦截，零漏检；同时只有约 24% 的图片需要人工审核，大幅减轻了工作量。

---

## 3. 数据集与标注工作

### 3.1 数据来源

数据来自某高校真实的晨读晨练签到系统，学生每日上传打卡照片，图片命名格式为 `{学号}-{日期}.jpeg`（如 `1-2026-04-13.jpeg`），同一天有多张照片时添加后缀（如 `80-2026-04-14-1.jpeg`）。

### 3.2 数据规模

| 类别 | 数量 | 比例 |
|------|------|------|
| 晨读 | 1491张 | 73.1% |
| 晨跑 | 525张 | 25.7% |
| 异常 | 24张 | 1.2% |
| **总计** | **2040张** | **100%** |

### 3.3 手工标注——2000+ 张图片的艰辛工作

整个项目的数据标注工作全部由我手工完成，这是项目中最耗时也最枯燥的部分。

**标注流程**：

1. 我使用 Python tkinter 开发了一个图形化标注工具（`scripts/feature_label_tool.py`），支持：
   - 三个主标签：晨读 / 晨跑 / 异常
   - 晨读特征（4项）：人脸、蓝色桌子、教室、投影幕布
   - 晨跑特征（8项）：人脸、跑道、天空、绿地、树木、旗杆、号码布、主席台
   - 异常原因：太暗、没有人、场景不对、背景模糊
   - 快捷键支持：方向键导航、数字键快速标注、跳过/跳转/随机/未标注筛选

2. 逐张查看每张图片，判断主类别和各个特征是否存在

3. 标注结果保存为 `data/labels.json`，包含每张图片的主标签、11维特征布尔值、异常原因、时间戳

**标注中的挑战**：

- **边界案例**：有些图片介于正常和异常之间（如光线偏暗但尚可辨认），需要反复斟酌
- **特征不确定性**：某些特征存在模糊地带（如远处隐约可见的跑道算不算"有跑道"？），需要统一标准
- **重复性劳动**：2000+ 张图片的标注工作耗时数周，中间需要不断休息以保持标注质量的一致性
- **异常样本稀缺**：在实际数据中异常样本极少（仅24张），虽然这减少了标注工作量，但也意味着模型训练时缺乏异常数据

**标注数据的质量保障**：

- 对不确定的样本进行了二次复核
- 建立了统一的标注标准文档（如"人脸"指可辨认的人脸特写，远景中的模糊人影不算）
- 异常样本额外标注了具体原因，便于后续分析异常类型的分布

### 3.4 异常类别详情

| 异常类型 | 描述 | 典型场景 |
|---------|------|---------|
| 光线过暗 | 图片整体亮度不足 | 宿舍关灯拍摄、凌晨光线极差 |
| 场景错误 | 不符合晨读或晨练场景 | 食堂、宿舍、走廊等 |
| 无人物 | 无法确认是否为本人签到 | 空教室、空操场、只有桌面 |
| 背景模糊 | 无法清晰辨认场景 | 严重运动模糊、失焦 |

### 3.5 数据集划分

| 数据集 | 数量 | 说明 |
|--------|------|------|
| 训练集 | ~1424张 | **仅包含正常样本（晨读+晨跑）** |
| 验证集 | ~164张 | 包含正常样本+部分异常 |
| 测试集 | ~408张 | 包含正常样本+全部异常 |

**关键决策——训练集不含异常样本**：

这是一个经过深思熟虑的设计选择：

1. 如果训练集包含异常，模型会尝试学习"异常"的决策边界，但异常样本太少且内部分异，导致边界不可靠
2. 不含异常的训练集让模型只学习两种正常场景的区分，学到的特征更纯粹
3. 异常图片进入训练好的模型后，由于在训练分布之外（OOD），自然会得到低置信度或少特征匹配的结果，从而被规则拦截
4. 这本质上是一种**开放集识别**的思想——模型只"认识"正常样本，任何不属于已知类别的东西都会表现出不确定性

---

## 4. 技术架构

### 4.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                          用户界面层                              │
├─────────────────────────────────────────────────────────────────┤
│  桌面 GUI (tkinter)                                            │
│  ├─ 文件夹选择 → 批量图片检测                                    │
│  ├─ 实时进度显示 + 结果表格                                      │
│  ├─ 图片预览 + 分类详情                                        │
│  ├─ 人工审核队列（快捷键 1/2/3/0 + 左右箭头）                     │
│  └─ JSON 报告导出                                              │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                          特征提取层                              │
├─────────────────────────────────────────────────────────────────┤
│  CLIP ViT-B/32（冻结权重）                                      │
│  ├─ 图像预处理：Resize 224×224 + Normalize                     │
│  ├─ 编码器前向传播                                              │
│  └─ 输出：512 维特征向量                                        │
└─────────────────────────────────────────────────────────────────┘
                               │
                          512维特征
                               │
                    ┌──────────┴──────────┐
                    ▼                     ▼
┌─────────────────────────┐  ┌──────────────────────────────────┐
│   MLP 主分类器           │  │   MLP 特征预测器（优化版）          │
│   512→256→128→2         │  │   512→512→256→128→11             │
│   (晨读/晨跑)           │  │   (残差连接 + LayerNorm + SiLU)    │
│   + Temperature=5.0     │  │   + Temperature=1.8              │
│   → softmax 概率        │  │   + 置信度钳制 0.95               │
│   → 预测标签 + 置信度    │  │   → sigmoid → 11维特征概率         │
└──────────┬──────────────┘  └──────────┬───────────────────────┘
           │                             │
           │    预测标签 + 置信度          │    11维特征概率
           │                             │
           └──────────┬──────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                     四规则三支决策引擎                             │
├─────────────────────────────────────────────────────────────────┤
│  规则1: 晨跑 且 置信度≥0.80 且 匹配特征≥5 → 自动通过              │
│  规则2: 晨读 且 置信度≥0.80 且 匹配特征≥3 → 自动通过              │
│  规则3: 匹配特征<3 → 待审核                                     │
│  规则4: 置信度<0.85 → 待审核                                    │
│  默认: 自动通过                                                 │
└─────────────────────────────────────────────────────────────────┘
                      │
              ┌───────┴───────┐
              ▼               ▼
         自动通过          待审核
                              │
                              ▼
                      人工审核队列
```

### 4.2 技术栈

| 组件 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 深度学习框架 | PyTorch | 2.0+ | 模型训练与推理 |
| 视觉模型 | CLIP ViT-B/32 | — | 512维特征提取 |
| 编程语言 | Python | 3.10+ | 全栈开发 |
| 图形界面 | tkinter | — | 检测系统GUI + 标注工具 |
| 数据处理 | pandas, numpy | — | 特征数据管理 |
| 模型评估 | scikit-learn | — | 指标计算 |

---

## 5. 模型设计详解

### 5.1 CLIP 特征提取

**模型选择**：OpenAI CLIP ViT-B/32

**选择理由**：

1. **强大的视觉表征能力**：CLIP 在 4 亿图文对上训练，学到了丰富的视觉语义特征，远超在有限数据上从头训练的 ResNet
2. **零样本泛化性**：即使没见过"号码布""蓝色桌子"等校园场景，CLIP 的特征空间也足够丰富，能捕获这些语义信息
3. **特征维度适中**：512 维特征向量，既保留了足够的视觉信息，又不会过于冗余
4. **冻结使用**：CLIP 权重完全冻结，只作为特征提取器，避免了微调带来的过拟合风险

**特征规格**：
- 特征维度：512 维
- 归一化：**不需要额外归一化**（CLIP `encode_image` 输出已经是 L2 归一化的）
- 输入尺寸：224×224 像素

**预处理流程**：
```python
from PIL import Image
import clip

clip_model, preprocess = clip.load("ViT-B/32", device=device)

img = Image.open(image_path).convert('RGB')
img_input = preprocess(img).unsqueeze(0).to(device)

with torch.no_grad():
    image_features = clip_model.encode_image(img_input)
```

**特征缓存策略**：

由于 CLIP 特征提取是项目中最耗时的步骤，我采用了一次提取、多次使用的策略：

1. 首次提取时，对所有图片计算 512 维特征，保存为 `data/clip_features_cpu.csv`
2. 训练和测试时直接加载 CSV 文件，无需重复计算
3. 这使得训练过程从"每轮需要 CLIP 前向传播"简化为"直接读取特征矩阵"，训练速度提升数十倍

### 5.2 MLP 主分类器（二分类）

**架构**：

```
输入层          隐藏层1         隐藏层2         输出层
[512维]  →  [256维+ReLU]  →  [128维+ReLU]  →  [2维]
              +Dropout(0.3)    +Dropout(0.3)
```

**代码**：
```python
class MLPClassifier(nn.Module):
    def __init__(self, input_dim=512, hidden_dim=256, output_dim=2, dropout=0.3):
        super().__init__()
        self.net = nn.Sequential(
            nn.Linear(input_dim, hidden_dim),       # 512 → 256
            nn.ReLU(),
            nn.Dropout(dropout),
            nn.Linear(hidden_dim, hidden_dim // 2), # 256 → 128
            nn.ReLU(),
            nn.Dropout(dropout),
            nn.Linear(hidden_dim // 2, output_dim)  # 128 → 2
        )

    def forward(self, x):
        return self.net(x)
```

**设计考量**：

- **为什么只有两层隐藏层**：CLIP 特征已经是高质量的语义特征，不需要很深的网络来进一步提取。浅层网络参数少，在小数据集上不易过拟合
- **为什么用 Dropout=0.3**：适度正则化，防止在 ~1400 张训练集上过拟合
- **为什么输出 2 维而非 3 维**：这是整个方案的核心——不直接预测异常。训练时只用正常样本，模型只需学会区分晨读和晨跑两种正常场景

**推理时的 Temperature Scaling**：

```python
logits = mlp_classifier(features)           # [batch, 2]
probs = torch.softmax(logits / 5.0, dim=1)  # 温度缩放
confidence = probs[0][pred_class].item()
```

T=5.0 使得 softmax 输出更加平滑，异常图片的置信度显著降低，便于规则拦截。详见[第6节](#6-温度缩放temperature-scaling)。

### 5.3 MLP 特征预测器（11维，优化版）

**这是整个方案中最核心的创新点**——通过训练一个独立的 MLP 来预测图中 11 个要素的存在概率，将"判断图片是否正常"这个大任务拆分为"判断每个要素是否存在"的 11 个小任务。

**架构**：

```
输入层         隐藏层1          隐藏层2          隐藏层3          输出层
[512维] → [512维+LN+SiLU] → [256维+LN+SiLU] → [128维+LN+SiLU] → [11维]
            +Dropout(0.25)    +Dropout(0.25)    +Dropout(0.25)
     │                                              ↑
     └─────────────── 残差连接（512→128）────────────┘
```

**代码核心结构**：
```python
class MLPFeaturesOptimized(nn.Module):
    def __init__(self, input_dim=512, hidden_dim=512, output_dim=11, dropout=0.25):
        super().__init__()
        self.fc1 = nn.Linear(input_dim, hidden_dim)            # 512 → 512
        self.ln1 = nn.LayerNorm(hidden_dim)
        self.fc2 = nn.Linear(hidden_dim, hidden_dim // 2)      # 512 → 256
        self.ln2 = nn.LayerNorm(hidden_dim // 2)
        self.fc3 = nn.Linear(hidden_dim // 2, hidden_dim // 4)  # 256 → 128
        self.ln3 = nn.LayerNorm(hidden_dim // 4)
        self.fc_out = nn.Linear(hidden_dim // 4, output_dim)    # 128 → 11
        self.residual = nn.Linear(hidden_dim, hidden_dim // 4)  # 512 → 128 (跳跃连接)

        self.temperature = 1.8          # 推理时温度
        self.confidence_clamp = 0.95    # 置信度上界
```

**关键设计**：

1. **残差连接**：从 fc1 输出（512维）直接线性投影到 fc3 输出维度（128维）并相加，缓解深层网络的梯度消失问题

2. **LayerNorm 替代 BatchNorm**：LayerNorm 对单个样本独立归一化，不受 batch size 影响，在 batch_size=32 时更稳定

3. **SiLU 激活函数（Swish）**：`SiLU(x) = x · sigmoid(x)`，比 ReLU 更平滑，在深层网络中训练更稳定，是现代网络架构的常用选择

4. **推理时温度缩放 + 置信度钳制**：
   ```python
   def forward(self, x, inference=False):
       ...
       if inference:
           logits = logits / self.temperature  # T=1.8，适度平滑
           probs = torch.sigmoid(logits)
           probs = torch.clamp(probs, max=self.confidence_clamp)  # 最高0.95
       return probs
   ```
   - 温度 T=1.8 使 sigmoid 输出更保守，避免特征概率过于极端（全0或全1）
   - 置信度钳制 0.95 防止任何特征维度出现 100% 置信度，保留不确定性

5. **Focal Loss + 最大熵正则化**：
   ```python
   class ConfidenceRegularizedLoss(nn.Module):
       def __init__(self, alpha=0.15, gamma=2.0):
           # alpha: 正样本权重（特征存在是少数情况）
           # gamma: Focal Loss聚焦参数，降低易分类样本的损失权重
   ```
   - Focal Loss 解决正负不平衡问题：11 维特征中大部分特征在大部分图片中不存在（如"号码布"只出现在晨跑图中），正负样本比约 1:4
   - 最大熵正则化鼓励模型输出更平滑的概率分布，避免过度自信

**11 维特征设计**：

| 索引 | 特征名 | 类别 | 判定依据 |
|------|--------|------|---------|
| 0 | 人脸 | 公共 | 画面中是否可见可辨认的人脸 |
| 1 | 蓝色桌子 | 晨读 | 教室中的蓝色课桌 |
| 2 | 教室 | 晨读 | 室内教室场景 |
| 3 | 投影幕布 | 晨读 | 教室前方的投影幕布 |
| 4 | 跑道 | 晨跑 | 塑胶跑道 |
| 5 | 天空 | 晨跑 | 户外天空 |
| 6 | 绿地 | 晨跑 | 草地或绿化带 |
| 7 | 树木 | 晨跑 | 户外树木 |
| 8 | 旗杆 | 晨跑 | 操场旗杆 |
| 9 | 号码布 | 晨跑 | 晨跑专用号码布（**权重×2**） |
| 10 | 主席台 | 晨跑 | 操场主席台 |

**号码布特征权重翻倍**：

```python
feature_probs[9] = min(feature_probs[9] * 2.0, 1.0)
```

号码布是晨跑最具有判别性的特征——只有晨跑时才佩戴号码布。然而在实际图片中号码布面积很小，特征预测器对其置信度往往偏低。通过权重翻倍，可以有效提高号码布对最终特征匹配数的贡献，让规则引擎更准确地识别晨跑场景。

### 5.4 双 MLP 协同机制

两个 MLP 并行工作，互不干扰，通过规则引擎组合输出：

| MLP | 输出 | 作用 |
|-----|------|------|
| 主分类器 | 预测标签（晨读/晨跑）+ 置信度 | 确定场景类别，提供置信度信号 |
| 特征预测器 | 11维特征概率 | 提供可解释的要素判断，支持特征匹配计数 |

**为什么不合并为一个网络**：

1. **任务异质性**：分类是多分类任务，特征预测是多标签任务，损失函数和优化策略不同
2. **训练效率**：分开训练可以针对每个任务使用最适合的损失函数（交叉熵 vs Focal Loss）
3. **推理灵活性**：可以独立调整每个模型的温度参数和决策阈值

---

## 6. 温度缩放（Temperature Scaling）

### 6.1 什么是温度缩放

温度缩放（Temperature Scaling）是一种后校准（post-calibration）技术，最早由 Guo 等人在 2017 年的论文 *"On Calibration of Modern Neural Networks"* 中系统研究。

其核心思想极为简洁：在 softmax 或 sigmoid 之前，将 logits 除以一个温度参数 T：

```python
# 分类器温度缩放
probs = torch.softmax(logits / T, dim=1)

# 特征预测器温度缩放
probs = torch.sigmoid(logits / T)
```

### 6.2 数学原理

**对 softmax 的影响**：

softmax 函数定义为：

$$p_i = \frac{\exp(z_i)}{\sum_j \exp(z_j)}$$

引入温度 T 后：

$$p_i = \frac{\exp(z_i / T)}{\sum_j \exp(z_j / T)}$$

- **T = 1**：标准 softmax，模型原始置信度
- **T > 1**：logits 被压缩，概率分布更平滑（更均匀），置信度降低
- **T < 1**：logits 被放大，概率分布更尖锐，置信度更高

**直观理解**：

假设模型对某张图片的原始 logits 为 `[5.0, 1.0]`（晨读 vs 晨跑）：

| T值 | P(晨读) | P(晨跑) | 置信度差 |
|-----|---------|---------|---------|
| 1.0 | 98.2% | 1.8% | 96.4% |
| 5.0 | 66.4% | 33.6% | 32.8% |
| 10.0 | 54.6% | 45.4% | 9.2% |

T 越大，两个类别的概率越接近，模型的"自信程度"越低。

**对 sigmoid 的影响**：

$$\sigma(z/T) = \frac{1}{1 + \exp(-z/T)}$$

T > 1 使 sigmoid 曲线更平缓，输出的概率更接近 0.5（更不确定）。

### 6.3 在本项目中的作用

**核心问题**：模型过度自信

训练好的 MLP 在正常图片上往往给出极高的置信度（>0.99），在异常图片上也常常给出看似不低的置信度（>0.90）。这使得仅凭置信度阈值难以区分正常和异常——因为正常和异常的置信度分布严重重叠。

**温度缩放如何解决**：

引入 T=5.0 后：

1. **正常图片**：原本 0.99 的置信度变为约 0.87-0.92，仍然高于阈值 0.85
2. **异常图片**：原本 0.92 的置信度变为约 0.65-0.80，低于阈值 0.85，被规则 4 拦截
3. **效果**：正常与异常的置信度分布被"拉开"了，使得简单的阈值判断就能有效区分

### 6.4 温度参数消融实验

我在验证集上进行了系统的温度消融实验：

| T值 | 漏检率 | 审核率 | 正常通过率 | 分析 |
|-----|--------|--------|-----------|------|
| 1.0 | **4.00%** | 21.99% | 73.22% | 异常样本置信度过高，规则4无法拦截 |
| 3.0 | 0.00% | 22.85% | 72.36% | 零漏检，审核率尚可 |
| **5.0** | **0.00%** | **24.37%** | **74.63%** | **零漏检且审核率最优平衡点** |
| 7.0 | 0.00% | 27.43% | 71.57% | 审核率开始偏高 |
| 10.0 | 0.00% | 30.70% | 68.30% | 审核率过高，正常样本被误拦截 |

**T=1.0 的问题**：

不使用温度缩放时，异常样本的置信度也很高（如 0.92），导致规则4（置信度 < 0.85）无法拦截，漏检率达到 4%。这 4% 的异常图片被错误地自动通过，在签到场景下是不可接受的。

**T=10.0 的问题**：

温度过高导致所有样本的置信度都被压低，大量正常样本的置信度也低于 0.85，审核率飙升至 30.70%，失去了自动化的意义。

**T=5.0 的选择理由**：

- 零漏检：所有异常样本的置信度都被压低到阈值以下
- 审核率可控：24.37% 的审核率意味着约 3/4 的图片可以自动处理
- 是漏检率和审核率之间的最优平衡点

### 6.5 特征预测器中的温度

特征预测器使用 T=1.8（而非 5.0），这是因为：

1. **任务性质不同**：特征预测是多标签二分类（每个特征独立判断存在/不存在），不需要像主分类器那样大幅压低置信度
2. **特征阈值已设为 0.60**：比主分类器的 0.85 低很多，不需要极端的温度值
3. **附加置信度钳制**：`torch.clamp(probs, max=0.95)` 确保没有任何特征维度出现 100% 置信度

---

## 7. 四规则三支决策系统

### 7.1 三支决策理论

三支决策（Three-Way Decision）是姚一豫教授提出的决策理论框架，将传统二支决策（是/否）扩展为三支（是/否/待定），在信息不充分时采取延迟决策。

在本项目中：
- **自动通过**（Positive Region）：模型高置信度且特征充分匹配，可以安全地自动放行
- **待审核**（Boundary Region）：模型不确定或特征匹配不足，需要人工确认
- **不存在"拒绝"**（Negative Region）：在签到场景下不存在完全拒绝的情况，异常图片最终由人工判定

### 7.2 四规则决策引擎

系统按以下优先级顺序执行四条规则：

```
输入：预测标签（晨读/晨跑）、置信度、11维特征概率

Step 1: 计算特征匹配数
    对预测标签对应的特征集合，统计概率 > 0.60 的特征数量

Step 2: 规则匹配（按优先级顺序）
    ┌─────────────────────────────────────────────────────────┐
    │ 规则1: 晨跑 且 置信度≥0.80 且 匹配特征≥5 → 自动通过      │
    │   → 晨跑场景特征丰富（8个），高置信+5特征=高可靠性         │
    ├─────────────────────────────────────────────────────────┤
    │ 规则2: 晨读 且 置信度≥0.80 且 匹配特征≥3 → 自动通过      │
    │   → 晨读场景特征较少（4个），3特征已覆盖大部分关键特征     │
    ├─────────────────────────────────────────────────────────┤
    │ 规则3: 匹配特征<3 → 待审核                              │
    │   → 特征太少说明图片信息量不足（模糊/异常/非目标场景）     │
    ├─────────────────────────────────────────────────────────┤
    │ 规则4: 置信度<0.85 → 待审核                              │
    │   → 低置信度意味着模型对分类不确定（可能是异常图）         │
    ├─────────────────────────────────────────────────────────┤
    │ 默认: 自动通过                                          │
    │   → 高置信+特征数3-4（晨读≥3已通过规则2，此为晨跑3-4特征） │
    └─────────────────────────────────────────────────────────┘
```

### 7.3 规则设计思路——降低漏检率和审核率

**为什么需要规则3（少特征拦截）**：

异常图片（如空教室、过暗照片）往往缺少正常场景的关键要素。特征预测器对这类图片输出的特征概率普遍较低，导致匹配特征数远低于正常图片。规则3正是利用这一特性来拦截异常。

**为什么需要规则4（低置信拦截）**：

由于主分类器训练时只见过正常样本，异常图片是分布外的（Out-of-Distribution）。根据 OOD 检测的理论，神经网络对分布外样本往往输出较低的 softmax 置信度。温度缩放 T=5.0 进一步放大了这一效应，使得异常样本的置信度更容易低于 0.85 的阈值。

**规则1/2 的快速通过机制**：

规则1和规则2是"绿色通道"——对于模型高置信度且特征匹配充分的图片，可以安全地自动通过，无需人工介入。这是降低审核率的关键。

**晨跑和晨读阈值不同的原因**：

- 晨跑特征集有 8 个特征（人脸、跑道、天空、绿地、树木、旗杆、号码布、主席台），要求匹配 5 个是合理的
- 晨读特征集只有 4 个特征（人脸、蓝色桌子、教室、投影幕布），要求匹配 3 个意味着至少 75% 的特征可见

### 7.4 规则有效性数据

基于验证集+测试集（632张图片，含25张异常）的评估：

| 规则 | 触发次数 | 正常样本 | 异常样本 | 精确率(异常) |
|------|---------|---------|---------|-------------|
| 规则1(晨跑快速通过) | 44 | 44 | 0 | — (放行规则) |
| 规则2(晨读快速通过) | — | — | — | — (放行规则) |
| 规则3(少特征拦截) | 74 | 72 | 2 | 2.7% |
| 规则4(低置信拦截) | 80 | 57 | 23 | 28.7% |

**分析**：

- **规则4 是拦截异常的主力**：23/25 的异常样本被低置信度规则拦截，说明温度缩放+置信度阈值是最有效的异常检测机制
- **规则3 是重要补充**：2 个异常样本置信度不够低（仍≥0.85），但匹配特征数少于 3，被规则3 拦截
- **规则1/2 零误放**：44 张自动通过的晨跑图片全部正确，证明了高置信+多特征组合的可靠性

### 7.5 参数优化

我使用 `tune_thresholds.py` 对关键阈值进行了网格搜索：

- `ALPHA_ACCEPT` ∈ [0.60, 0.65, ..., 0.90]（步长 0.05）
- `ALPHA_AUTO_PASS` ∈ [0.60, 0.65, ..., 0.90]（步长 0.05）
- `FEATURE_THRESHOLD` ∈ [0.30, 0.35, ..., 0.70]（步长 0.05）

**优化目标**：漏检率 = 0 且 审核率最低

**最终参数**（修改 `src/config.py` 即可全局生效）：

| 参数 | 值 | 说明 |
|------|-----|------|
| CLASSIFIER_TEMPERATURE | 5.0 | 主分类器 softmax 温度（不需重训练） |
| FEATURE_TEMPERATURE | 1.8 | 特征预测器 sigmoid 温度（不需重训练） |
| ALPHA_REVIEW | 0.85 | 置信度阈值（规则4） |
| ALPHA_AUTO_PASS | 0.80 | 自动通过最低置信度（规则1/2） |
| FEATURE_THRESHOLD_READ | 0.66 | 晨读特征认定阈值（人脸/蓝桌/教室/幕布） |
| FEATURE_THRESHOLD_RUN | 0.60 | 晨跑特征认定阈值（跑道/树木/草地等7个） |
| MIN_FEATURES | 3 | 最少匹配特征数（规则3） |
| RUN_FEATURE_THRESH | 5 | 晨跑快速通过特征数（规则1） |
| READ_FEATURE_THRESH | 3 | 晨读快速通过特征数（规则2） |

> ⚠️ **重要**：以上两个温度参数只在推理时生效（`logit / temperature`），**修改温度不需要重新训练模型**。
> 所有参数集中在 `src/config.py`，修改后 `checkin_system.py`、`train_mlp.py`、`test_current.py` 自动同步。

---

## 8. 训练流程

### 8.1 统一训练脚本

项目使用 `train_mlp.py` 作为统一训练入口，同时训练主分类器和特征预测器。

### 8.2 主分类器训练

| 参数 | 值 | 说明 |
|------|-----|------|
| 优化器 | Adam | lr=0.001, weight_decay=1e-4 |
| 学习率调度 | StepLR | 每 20 轮衰减 50%（step=20, gamma=0.5） |
| 损失函数 | CrossEntropyLoss | 标准二分类交叉熵 |
| Batch Size | 32 | 小批量训练 |
| Epochs | 100 | 最大训练轮数 |
| Dropout | 0.3 | 防止过拟合 |
| 最佳模型选择 | 验证集准确率最高 | 保存验证集上表现最好的模型 |

**关键点**：

- 训练数据只包含晨读和晨跑，异常样本被排除
- 标签映射：晨读=0, 晨跑=1
- 使用 StepLR 而非 CosineAnnealing，因为二分类任务相对简单，不需要复杂的学习率策略

### 8.3 特征预测器训练

| 参数 | 值 | 说明 |
|------|-----|------|
| 优化器 | AdamW | lr=0.001, weight_decay=1e-4（解耦权重衰减） |
| 学习率调度 | CosineAnnealingWarmRestarts | T_0=15, T_mult=2（周期性重启） |
| 损失函数 | ConfidenceRegularizedLoss | Focal Loss(alpha=0.15, gamma=2.0) + 最大熵正则 |
| Batch Size | 32 | 小批量训练 |
| Epochs | 250 | 最大训练轮数 |
| Dropout | 0.25 | 比主分类器略低 |
| 梯度裁剪 | max_norm=5.0 | 防止梯度爆炸 |
| Early Stopping | patience=25 | 验证损失 25 轮不改善则停止 |
| 数据增强 | 高斯噪声(noise_std=0.02) | 在 512 维 CLIP 特征上添加噪声 |

**Focal Loss 的作用**：

11 维特征中，正样本（特征存在）远少于负样本（特征不存在）。例如"号码布"只在约 25% 的图片中出现。Focal Loss 通过降低易分类样本（大量负样本）的损失权重，使模型更关注难分类样本（少量正样本），有效缓解了类别不平衡问题。

**高斯噪声数据增强**：

```python
def augment_features(features, noise_std=0.02):
    noise = torch.randn_like(features) * noise_std
    return features + noise
```

在 CLIP 特征向量上添加标准差 0.02 的高斯噪声，模拟特征提取的微小波动，增加训练数据的多样性，防止过拟合。

**CosineAnnealingWarmRestarts**：

使用余弦退火带热重启的学习率策略，T_0=15 表示第一个周期 15 轮，之后每轮周期翻倍（15→30→60...）。热重启可以帮助模型跳出局部最优，在多标签预测任务中尤其有效。

### 8.4 验证与评估

训练完成后，使用 `validate_with_three_way_decision()` 进行端到端的三支决策验证：

```python
# 对每个验证样本：
1. 提取 CLIP 特征
2. 主分类器预测 + 温度缩放
3. 特征预测器预测
4. 四规则决策
5. 统计漏检率、审核率、正常通过率
6. 分析各规则触发情况
```

最终使用 `evaluate_model()` 在测试集上评估模型性能。

---

## 9. 实验结果

### 9.1 各方案对比总览

| 阶段 | 方案 | 整体准确率 | 异常识别能力 | 漏检率 | 可解释性 | 结论 |
|------|------|-----------|------------|--------|---------|------|
| 一 | ResNet18 三分类 | 94.12% | 33.33% | 66.7% | ❌ | 异常类几乎无法识别 |
| 二 | ResNet50 三分类/二分类 | 95.3%/96.8% | 37.5%/— | 62.5%/100%* | ❌ | 更大模型无法解决数据问题 |
| 三 | CLIP 零样本 | 76.35% | 41.67% | 58.3% | ⚠️ | 语义鸿沟，无法描述"异常" |
| 四 | CLIP + 单MLP | ~97% | 不足 | 仍有漏检 | ❌ | 黑箱，不可解释 |
| **五** | **CLIP + 双MLP + 三支决策** | **~99%** | **100%** | **0%** | **✅** | **完美满足所有要求** |

*注：ResNet50 二分类的"100%漏检"意味着所有异常都被误分类为正常，因为模型根本不识别异常。

### 9.2 最终方案测试结果

```
============================================================
【规则有效性验证报告】
============================================================

【规则说明】
  规则1: 晨跑 且 置信度≥0.80 且 匹配特征≥5 → 自动通过
  规则2: 晨读 且 置信度≥0.80 且 匹配特征≥3 → 自动通过
  规则3: 匹配特征<3 → 待审核
  规则4: 置信度<0.85 → 待审核

【核心指标】
  总样本数: 632
  正常样本: 607 | 异常样本: 25
  漏检率: 0.00% (漏检0/25)
  审核率: 22.83% (待审核144/632)
  正常通过率: 77.17% (通过467/607)

【规则触发统计】
  规则1(晨跑快速通过): 44张
  规则2(晨读快速通过): 若干张
  规则3(少特征拦截): 74张
  规则4(低置信拦截): 80张
  自动通过(默认): 478张

【规则有效性分析】
  1. 规则1/2 (高置信+多特征自动通过):
     - 触发的自动通过全部为正常样本
     - 规则可靠性: 100%

  2. 规则4 (低置信度拦截):
     - 触发80次, 其中正常样本57张, 异常样本23张
     - 异常识别精确率: 28.7%
     - 是拦截异常的主力规则

  3. 规则3 (少特征拦截):
     - 触发74次, 其中正常样本72张, 异常样本2张
     - 异常识别精确率: 2.7%
     - 作为规则4的重要补充，拦截了2个置信度不低的异常
```

### 9.3 性能指标总结

| 指标 | 目标 | 实际 | 状态 |
|------|------|------|------|
| 漏检率 | 0% | 0.00% | ✅ |
| 人工审核率 | <30% | 22.83% | ✅ |
| 正常通过率 | >70% | 77.17% | ✅ |
| 分类准确率 | >95% | ~99% | ✅ |
| 可解释性 | 支持 | 11维特征概率 | ✅ |

---

## 10. 系统实现

### 10.1 主检测系统（`src/checkin_system.py`）

基于 tkinter 的桌面 GUI 应用，核心功能：

1. **文件夹选择**：选择待检测的图片文件夹
2. **批量检测**：对文件夹内所有图片执行 CLIP 特征提取 + 双 MLP 预测 + 四规则决策
3. **结果表格**：显示每张图片的预测标签、置信度、匹配特征数、决策结果
4. **图片预览**：点击表格行可预览对应图片
5. **人工审核队列**：待审核图片进入审核队列，支持快捷键操作：
   - `1`：标记为晨读
   - `2`：标记为晨跑
   - `3`：标记为异常
   - `0`：跳过
   - `←/→`：前后翻页
6. **审核追踪**：记录审核结果，区分"自动通过被纠正"（算模型错误）和"待审核被确认"（不算模型错误）
7. **JSON 报告导出**：生成包含所有检测结果的 JSON 文件

### 10.2 标注工具（`scripts/feature_label_tool.py`）

图形化标注工具，支持：
- 三个主标签快速切换（快捷键 1/2/3）
- 11 维特征复选框标注
- 异常原因勾选
- 导航快捷键（方向键、K跳过、G跳转、R随机、U未标注）
- 标注进度统计和筛选

### 10.3 预处理脚本（`scripts/preprocessing.py`）

四个核心功能：
1. **图像质量检查**：检测过暗/过亮/尺寸异常的图片
2. **CLIP 特征提取**：批量提取 512 维特征并缓存
3. **数据集划分**：分层采样，确保训练集不含异常
4. **数据集分析**：统计学生/日期/类别分布

### 10.4 参数调优脚本（`tune_thresholds.py`）

网格搜索最优阈值组合：
- 固定 Temperature=5.0
- 搜索 ALPHA_ACCEPT、ALPHA_AUTO_PASS、FEATURE_THRESHOLD 的最优组合
- 目标：漏检率=0 且审核率最低

---

## 11. 部署说明

### 11.1 环境准备

```bash
# 方式一：conda环境
conda env create -f environment.yml
conda activate checkin_detection

# 方式二：手动安装
conda create -n checkin_detection python=3.10
conda activate checkin_detection
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
pip install git+https://github.com/openai/CLIP.git
pip install scikit-learn pandas numpy pillow

# 方式三：Windows一键安装
setup.bat
```

### 11.2 使用流程

```bash
# 1. （可选）训练模型
python train_mlp.py

# 2. （可选）参数调优
python tune_thresholds.py

# 3. （可选）验证当前规则
python test_current.py

# 4. 启动检测系统
python src/checkin_system.py

# 5. （可选）人工标注工具
python scripts/feature_label_tool.py
```

### 11.3 文件依赖关系

```
运行检测系统需要的文件：
  src/checkin_system.py          ← 主程序入口
  src/models/mlp.py              ← 主分类器模型
  src/models/mlp_features_optimized.py  ← 特征预测器模型
  data/mlp_classifier.pt         ← 主分类器权重
  data/mlp_features_optimized.pt ← 特征预测器权重
  data/labels.json               ← 标签（用于显示参考）
```

---

## 12. 总结与反思

### 12.1 技术收获

1. **CLIP 特征的威力**：预训练大模型的特征提取能力远超在小数据集上从头训练的 CNN。即使 CLIP 没有见过"号码布"或"蓝色桌子"，其特征空间仍然能编码这些语义信息，使得轻量级的 MLP 就能完成分类任务

2. **任务拆分的智慧**：老师的建议——将"判断图片是否正常"这个大任务拆分为"判断每个要素是否存在"的多个小任务——是整个项目的转折点。这不仅解决了可解释性问题，还让规则引擎有了可靠的决策依据

3. **温度缩放的重要性**：在后校准领域，温度缩放是一个简单但强大的工具。在本项目中，T=5.0 的温度参数是零漏检的关键——它让异常样本的置信度显著降低，使得简单的阈值规则就能拦截所有异常

4. **三支决策的实用性**：在信息不完全时选择"延迟决策"而非"强行决策"，是一种务实的工程智慧。宁可多审核 24% 的图片，也不放过任何一个异常

5. **规则与模型的互补**：纯模型方案和纯规则方案都有局限，但将两者结合可以发挥各自优势——模型负责特征提取和初步判断，规则负责异常拦截和决策把关

### 12.2 工程经验

1. **数据标注是最大的工程挑战**：2000+ 张图片的手工标注耗时数周，但高质量的数据是项目成功的基础。标注工具的开发大大提高了标注效率

2. **失败是成功之母**：每一个失败的方案（ResNet18 → ResNet50 → CLIP零样本 → CLIP+单MLP）都让我对问题有了更深的理解，最终指引我找到了正确的方案

3. **简单往往更可靠**：最终方案没有使用最深的网络或最复杂的算法，而是 CLIP（冻结）+ 两个小 MLP + 规则引擎。简单意味着可控、可调试、可解释

4. **参数调优需要系统化**：网格搜索虽然朴素，但在参数空间较小时是最可靠的方法。消融实验帮助我理解了每个参数的作用和影响

### 12.3 不足与未来方向

1. **异常样本过少**：24 张异常样本限制了方案评估的统计可靠性，未来应收集更多异常样本
2. **特征维度可扩展**：11 维特征是手工设计的，未来可考虑自动发现判别性特征
3. **在线学习能力**：当前系统不具备从人工审核结果中学习的能力，增加在线学习可进一步提升系统性能
4. **多模型集成**：可引入多个特征提取器（如 DINOv2、SigLIP）进行特征融合，提升鲁棒性

---

**文档版本**：v8.0
**更新日期**：2026-06-11
**作者**：晨读晨练签到检测系统开发者
