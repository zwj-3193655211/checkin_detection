# 晨读晨练签到检测系统 - Python 依赖（精确钉版）
# 运行环境: conda env 'checkin_detection' (Python 3.10.20, Windows)
# 激活:     conda activate checkin_detection
# 安装:     pip install -r requirements.txt
#
# 重要说明（踩坑记录，复现环境务必遵守）：
# 1) 当前专属环境 torch 为 CPU 版 (2.11.0+cpu)。本文件仅锁主版本 2.11.0；
#    若需 CUDA 版（有 GPU 时更快），从 https://download.pytorch.org/whl 安装对应 wheel，
#    默认 pip 装到的就是 CUDA 版。本项目 MLP 训练/调优在 CPU 上即可秒级完成。
# 2) Pillow 必须用 pip 安装（不要用 conda-forge 构建）：conda 版 Pillow 与 torch 的
#    DLL 搜索路径冲突，会先 import torch 再 import PIL 时报
#    "DLL load failed while importing _imaging: 操作系统无法运行 %1"。
#    已锁定 pip 的 12.3.0 wheel（原生库私有打包，绕开冲突）。
# 3) clip (OpenAI CLIP) 通过 git 源码安装。

# ---- 核心 ----
torch==2.11.0
torchvision==0.26.0
numpy==2.2.5
pandas==2.3.3
Pillow==12.3.0
tqdm==4.67.3

# ---- 视觉编码器 ----
timm==1.0.28            # SigLIP2-NaFlex / ViT-Tiny 加载 (src/encoders.py, scripts/train_vit_tiny.py)
safetensors==0.8.0      # 权重读取 (scripts/train_vit_tiny.py)
transformers==4.57.6    # AutoModel / AutoImageProcessor (src/encoders.py, scripts/extract_siglip_features.py)
# 注: 此 git 依赖安装时需能访问 github.com。当前部分网络环境下 github 不可达
# (git clone 报 Connection was reset)，会导致从零 pip install 卡在这一行；
# 可临时走代理 (如 http://127.0.0.1:7897) 或先单独安装。专属环境内 clip 1.0 已装好，无需重装。
git+https://github.com/openai/CLIP.git   # import clip

# ---- 报告生成 ----
python-docx==1.2.0      # import docx (scripts/update_report.py, scripts/update_report2.py)
