# 实验记录：11独立MLP特征预测器 vs 共享MLP（2026-08-10）

## 背景
数据集由 2057 张扩充至 2067 张（+10 张已标注新图，晨读 1497 / 晨跑 546 / 异常 24）。
重新提取 CLIP 特征、重划分（Train 1429 / Val 317 / Test 321，异常全部在验证/测试集）、
重训双 MLP，并对比「11 独立 MLP」新方案。

## 方案说明
| 方案 | 结构 | 说明 |
|------|------|------|
| baseline | MLPFeaturesOptimized 512→512→256→128→11（共享+残差） | 现系统方案，Focal + 熵正则(α=0.15) |
| 11-MLP v1 | 11× (512→128→64→1)，alpha=1-正类占比(0.05~0.95) | 极端 alpha 加权，无熵正则 |
| 11-MLP v2 | 11× (512→256→128→1)，alpha∈[0.3,0.7]，熵正则β=0.15 | 修正版，epochs 200 |

## 结果1：特征准确率（测试集，阈值0.5）
| 特征 | baseline | 11-MLP v2 |
|------|----------|-----------|
| 人脸 | 95.02 | 97.51 ▲ |
| 蓝色桌子 | 95.95 | 96.57 ▲ |
| 教室 | 97.20 | 96.57 |
| 投影幕布 | 97.51 | 97.20 |
| 跑道 | 95.64 | 95.02 |
| 天空 | 96.88 | 97.82 ▲ |
| 绿地 | 94.70 | 95.95 ▲ |
| 树木 | 95.95 | 95.33 |
| 旗杆 | 96.26 | 97.51 ▲ |
| 号码布 | 98.44 | 98.44 = |
| 主席台 | 97.51 | 97.20 |

→ 11-MLP v2 特征准确率与 baseline 基本持平（5/11 略优）。

## 结果2：三支决策（固定阈值 0.66/0.60）
| 数据集 | baseline | 11-MLP v1 | 11-MLP v2 |
|--------|----------|-----------|-----------|
| 验证集 review | 25.55% | 15.14% | 83.28% |
| 测试集 review | 25.86% | 15.89% | 85.05% |
| 测试集 miss | 0% | 0% | 0% |

→ v1 概率过度自信（特征数虚高→审核率虚低），v2 概率过度保守（特征数虚低→审核率虚高）。
**特征概率校准（温度/阈值匹配）决定审核率，而非模型结构。**

## 结果3：每特征独立阈值（坐标下降搜索，验证集）
| 模型 | 阈值下限 | 测试集 review | 测试集 miss |
|------|---------|--------------|------------|
| baseline | 0.30 | 6.85% | 0% |
| baseline | 0.40 | 6.85% | 8.33% ❌ |
| 11-MLP | 0.30 | 10.28% | 8.33% ❌ |
| 11-MLP | 0.40 | 14.02% | 8.33% ❌ |

→ 阈值搜索可大幅降低审核率，但对临界异常样本（idx=196，conf=0.903、4个晨读特征概率
0.51~0.67）高度敏感：蓝色桌子阈值 ≤0.40 时该样本匹配数=3 被放行（漏检）。
**现配置 0.66/0.60 对临界样本最安全（该样本匹配数=2 <3 被 R3 拦截）。**

## 结论
1. **11 独立 MLP 不采用**：特征准确率与 baseline 打平，但概率校准更差、
   对异常样本泛化更弱（验证集 miss=0 但测试集漏检 1 个），且无结构优势。
   共享网络能利用特征相关性（教室/投影幕布/蓝色桌子互相强化），
   小样本特征（号码布 56 正）训练也更稳。
2. **每特征独立阈值是有效改进方向**，但受限于 24 个异常样本，搜索存在过拟合风险；
   在收集更多异常样本前，维持现配置 0.66/0.60 更稳健。
3. **最终采用**：baseline 双 MLP（新数据重训）+ 现配置阈值。
   测试集：漏检率 0%（0/12）、审核率 25.86%、自动通过率 74.14%、特征准确率 96.46%。

## 文件
- `train_features_11mlp.py`  — 11 独立 MLP 训练脚本（v2 输出 data/mlp_features_11x_v2.pt）
- `compare_features.py`      — 两方案三支决策 + 特征准确率对比
- `tune_feature_thresholds.py` — 每特征独立阈值坐标下降搜索
- `diagnose_thresholds.py`   — 阈值诊断（规则触发/匹配数/异常详情）
